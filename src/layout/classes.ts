import { css } from '@emotion/css';
import { Primary } from '@/styles/theme';

export const ThemeClass = css({
  color: Primary['dark']['beige'].text,
  backgroundColor: Primary['dark']['beige'].background,
});
