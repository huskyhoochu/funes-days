import { css } from '@emotion/css';
import { Theme } from '@/styles/theme';

export const ThemeClass = css({
  color: Theme['dark']['beige'].text,
  backgroundColor: Theme['dark']['beige'].background,
});
